$(".list-group-item-action.active").removeClass('active');
<% if @section == 'awaiting_ads'%>
$("#settings-detail").replaceWith("<%= escape_javascript(render(:partial => 'settings/awaiting_ads')) %>");
$("#awaiting_ads-nav").addClass('active');
<%end%>
